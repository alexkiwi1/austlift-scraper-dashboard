version: '3.8'

services:
  austlift-scraper-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_BASE_URL=http://10.100.7.1:8000
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true



